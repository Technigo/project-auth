{
  "name": "project-auth-parent",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend"
  },
  "dependencies": {
    "express-list-endpoints": "^6.0.0",
    "multer": "^1.4.5-lts.1"
  }
}
