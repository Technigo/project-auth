import { createSlice } from "@reduxjs/toolkit"; 

export const society = createSlice({
    name: "society", 
    initialState: {
        username: null, 
        accessToken: null
    }, 

    reducers: {
        
    }
})