import React, { useState } from 'react'
import styled from 'styled-components'

//Try GET request for '/session' here. Content or error message is displayed.

export const Content = () => {
  return (
    <h2>Here is the content!</h2>
  )
}