import React from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';

export const App = () => {
  return (
    <dBrowserRouter>
      <Routes>
        
      </Routes>
    </dBrowserRouter>
  );
}
