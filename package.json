{
  "name": "project-auth-parent",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "js": "^0.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}