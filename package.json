{
  "name": "project-auth-parent",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend"
  },
  "dependencies": {
    "express-list-endpoints": "^5.0.0",
    "regex": "^0.1.1"
  }
}
